import assert from"node:assert/strict";import{test}from"node:test";import{assertLiveDevnetCanary}from"../src/e2e/liveDevnetCanaryPolicy";import{AGENT_SCENARIO_BLOCKERS,RUNNABLE_E2E_SCENARIOS,UNSUPPORTED_AGENT_SCENARIOS}from"../src/e2e/e2eTypes";
const actor={syntheticActorId:"synthetic-human-a",accountId:"10000000-0000-4000-8000-000000000001",actorClass:"synthetic_test"as const,actorKind:"human"as const,testOrigin:"codex_e2e"as const,createdAt:"2026-08-15T00:00:00.000Z"};
test("scenario vocabulary is complete and agent families identify missing primitives",()=>{assert.equal(RUNNABLE_E2E_SCENARIOS.length,9);assert.deepEqual(UNSUPPORTED_AGENT_SCENARIOS,["human-to-agent","agent-to-human","agent-to-agent"]);for(const scenario of UNSUPPORTED_AGENT_SCENARIOS)assert(AGENT_SCENARIO_BLOCKERS[scenario].length>0);});
test("live canary safety policy accepts only explicit bounded synthetic Devnet input",()=>{const input={mode:"LIVE_DEVNET_CANARY"as const,explicitLiveFlag:true,liveConfirmation:"I_UNDERSTAND_THIS_SUBMITS_DEVNET_USDC",network:"solana-devnet",amountRaw:1000n,source:actor,destination:{...actor,syntheticActorId:"synthetic-human-b"},submissionEnabled:true,reconciliationEnabled:true,existingCommitmentCount:0};assert.doesNotThrow(()=>assertLiveDevnetCanary(input));for(const invalid of[{...input,network:"solana-mainnet"},{...input,amountRaw:1001n},{...input,existingCommitmentCount:1},{...input,explicitLiveFlag:false},{...input,submissionEnabled:false}])assert.throws(()=>assertLiveDevnetCanary(invalid));});


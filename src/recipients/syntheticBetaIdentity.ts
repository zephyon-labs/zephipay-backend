import {createHash} from "node:crypto";
export type SyntheticBetaIdentity=Readonly<{syntheticId:string;normalizedName:string;displayName:string;username:string;identitySource:"SYNTHETIC_BETA"}>;
export interface SyntheticBetaIdentityStore{findById(id:string):Promise<SyntheticBetaIdentity|undefined>;findByName(name:string):Promise<SyntheticBetaIdentity|undefined>;claim(value:SyntheticBetaIdentity):Promise<SyntheticBetaIdentity>}
export function normalizeSyntheticBetaName(value:unknown){if(typeof value!=="string")throw new Error("INVALID_SYNTHETIC_NAME");const display=value.trim().normalize("NFKC"),bytes=new TextEncoder().encode(display);if(!display||bytes.length>64||/[\p{Cc}\p{Cf}\p{Cs}]/u.test(display)||!/[\p{L}\p{N}]/u.test(display))throw new Error("INVALID_SYNTHETIC_NAME");const normalized=display.toLocaleLowerCase("en-US");return{displayName:display,normalizedName:normalized}}
export function syntheticBetaIdentity(value:unknown):SyntheticBetaIdentity{const{displayName,normalizedName}=normalizeSyntheticBetaName(value),hex=createHash("sha256").update(`zephipay:synthetic-beta:v1:${normalizedName}`,"utf8").digest("hex"),uuid=`${hex.slice(0,8)}-${hex.slice(8,12)}-5${hex.slice(13,16)}-a${hex.slice(17,20)}-${hex.slice(20,32)}`;return Object.freeze({syntheticId:uuid,normalizedName,displayName,username:displayName,identitySource:"SYNTHETIC_BETA"})}

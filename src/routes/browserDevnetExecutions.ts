import{Router,type RequestHandler,type Response}from"express";import{externalPrincipalFrom}from"../auth/authMiddleware";import{parsePaymentIntentId}from"../payments/paymentIntentValidation";import{BrowserDevnetError,BrowserDevnetExecutionService}from"../devnet/browserDevnetExecution";
export function createBrowserDevnetExecutionsRouter(input:{service:BrowserDevnetExecutionService;readAuth:readonly RequestHandler[];writeAuth:readonly RequestHandler[];mutationLimiter?:RequestHandler;readLimiter?:RequestHandler}){const router=Router();router.use((_req,res,next)=>{res.set("Cache-Control","no-store, private");res.set("Pragma","no-cache");next();});router.post("/:id/devnet/execute",...input.writeAuth,...(input.mutationLimiter?[input.mutationLimiter]:[]),async(req,res)=>{try{const result=await input.service.execute(externalPrincipalFrom(res),parsePaymentIntentId(String(req.params.id)),req.body);return res.status(result.created?202:200).json({ok:true,created:result.created,execution:result.execution,requestId:res.locals.requestId});}catch(error){return handle(error,res);}});router.get("/:id/devnet/execution",...input.readAuth,...(input.readLimiter?[input.readLimiter]:[]),async(req,res)=>{try{return res.json({ok:true,execution:await input.service.find(externalPrincipalFrom(res),parsePaymentIntentId(String(req.params.id))),requestId:res.locals.requestId});}catch(error){return handle(error,res);}});return router;}
function handle(error:unknown,res:Response){if(error instanceof BrowserDevnetError){const status=error.kind==="INVALID"?400:error.kind==="ACCESS_DENIED"?403:error.kind==="NOT_FOUND"?404:error.kind==="DISABLED"?503:409;return res.status(status).json({ok:false,error:error.message,code:`DEVNET_${error.kind}`,requestId:res.locals.requestId});}throw error;}

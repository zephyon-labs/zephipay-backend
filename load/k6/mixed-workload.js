import {get,principal,smokeOptions} from "./shared.js";export const options=smokeOptions;export default function(){const p=principal(),n=__ITER%5;if(n===0)get("/api/account/me",p.account,"account_me");else if(n===1)get("/api/activity?limit=20",p.read,"activity_read");else if(p.intentId)get(`/api/payment-intents/${p.intentId}${n===2?"":n===3?"/execution":"/receipt"}`,p.read,"payment_read");else get("/api/account/identity",p.account,"payment_identity");}

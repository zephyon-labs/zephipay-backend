import http from "k6/http";import {base,expect,headers,principal,smokeOptions} from "./shared.js";export const options=smokeOptions;export default function(){const p=principal();if(!p.intentId)return;const body=JSON.stringify({requestHash:p.requestHash,expectedVersion:p.expectedVersion});for(let i=0;i<2;i++)expect(http.post(`${base}/api/payment-intents/${p.intentId}/execute`,body,{headers:headers(p.write,{"Content-Type":"application/json"}),tags:{route:"execute_replay"}}),200,"execute_replay");const denied=http.get(`${base}/api/payment-intents/${p.intentId}/receipt`,{headers:headers(p.otherRead),tags:{route:"cross_account_receipt"}});expect(denied,404,"cross_account_receipt");}

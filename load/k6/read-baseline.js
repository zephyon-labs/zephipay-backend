import {sleep} from "k6";import {base,expect,get,principal,smokeOptions} from "./shared.js";import http from "k6/http";export const options=smokeOptions;export default function(){const p=principal();if(__ENV.SAMPLE_READY!=="false"&&__ITER%10===0)expect(http.get(`${base}/health/ready`,{tags:{route:"health_ready"}}),200,"health_ready");get("/api/account/me",p.account,"account_me");if(p.intentId){get(`/api/payment-intents/${p.intentId}`,p.read,"intent_read");get(`/api/payment-intents/${p.intentId}/execution`,p.read,"execution_read");get(`/api/payment-intents/${p.intentId}/receipt`,p.read,"receipt_read");}get("/api/activity?limit=20",p.read,"activity_read");sleep(Number(__ENV.PACE_SECONDS||0));}
